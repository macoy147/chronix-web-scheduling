# MongoDB connection URI (use Atlas; do NOT commit real credentials)
MONGODB_URI="your_mongodb_connection_string_here"

# Optional
PORT=3001
# e.g. any JWT secret you may add in future:
JWT_SECRET="replace_me"